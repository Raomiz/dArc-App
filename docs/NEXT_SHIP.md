# ō — next ship

House note for agents and the founder. Rooted in what this repository
actually contains on `main` (Grok export `229ef3c`) plus the Field seat
in this change. Do not invent a Flutter app, a Play listing, or a
paywall that is not in the tree.

ō and Raz are **equal product alternatives**. This file is only ō’s
near-term path because hardware blocks Raz / Unreal grind.

## 1. What exists now

`Raomiz/dArc-App` is a **TanStack Start / React** private atlas, not a
Flutter package. One public commit (`Export from Grok`). The sealed
canvas opens only after a bound Google identity (`jdraomiz@gmail.com`).
Unsigned visitors see a threshold. Precincts are Cycle, Site, and Field.
Comments persist per sealed user. Raz was already a Field watch. ō is
now a Field watch too (`named`, not `ready`). There is no `pubspec.yaml`,
no Dart, no Android Gradle project, no IAP, no store listing assets.

## 2. Blockers to ship

- **Wrong stack for the stated Flutter package.** Scaffolding Flutter
  here would overwrite or fork a working atlas. The founder PC is below
  standard; do not assume a local Android / Unreal toolchain.
- **ō has no runnable loop.** The watch is a mark on the table. Tapping
  it only opens the existing pin-comment form.
- **Public investor door is thin.** This repo had no README. `d-arc.io`
  is the Site mark; live fetch of the apex has been 403 /
  `DEPLOYMENT_NOT_FOUND` in house memory. Vercel team
  `raomizs-projects` exists; this connector listed **no** projects.
- **Auth is preview-brokered.** Google sign-in is wired for the Grok
  canvas (`GROK_*` broker). A store-path Android binary is not.
- **Cursor budget is Pro+, not Ultra.** Next work must stay one
  finishable slice.

## 3. The single next ship (this PR)

**Seat ō on the Field beside Raz, and publish a public README + this
checklist.**

Why this, not Flutter / paywall / Play copy:

- The Field already had the watch pattern. ō was named in Layer 0
  (`notThis`: “The digital companion in the field”) and missing from
  the table — so a demo showed Raz as the only field product.
- A Flutter hello-world, paywall stub, or store listing would be
  speculative and would not run in this repo’s toolchain.
- A public README turns the GitHub dump into investor-readable
  evidence of what is real.

**Do not do next:** start `flutter create`, add billing, write Play
store copy, or grind Raz.

## 4. How to verify

1. Open the atlas as the bound identity (or live preview owner).
2. Tap **Field** (key `3` / bottom nav). Both **ō** (`named`) and
   **Raz** (`seated`) are featured watches.
3. Tap ō — the pin-comment form is the only action. That is observed
   reality, not a companion loop.
4. `npm test` includes `src/lib/house/atlas.test.ts` (equal watches;
   ō is not marked built).
5. README on the public repo states the stack honestly.

## After this PR — one finishable slice

When the next agent or the founder picks up ō, choose **one**:

1. **Pocket demo (preferred).** Keep this web atlas. Get a live URL
   the founder can open on an Android phone and add to the home
   screen. Show Field: ō + Raz. That is demoable evidence without
   Unreal or Flutter.
2. **ō loop, still web.** One screen that does one real-world action
   (e.g. pin a Field note from a phone-width viewport) — only if the
   door in (1) already opens.
3. **Flutter later, new tree.** Only after (1), and only in a repo
   that is actually Dart. Do not drop `pubspec.yaml` onto this
   TanStack app.

Money / investor signal from (1): a phone screenshot of the sealed
table with both products named. Not a store listing of an empty APK.
